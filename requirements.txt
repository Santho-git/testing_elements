selenium==4.14.0
pytest==7.4.2
pytest-html==3.2.0
pytest-xdist==3.2.1
webdriver-manager==4.8.0

| Package             | Purpose                                                                                |
| ------------------- | -------------------------------------------------------------------------------------- |
| `selenium`          | Core library for browser automation                                                    |
| `pytest`            | Test runner for executing your test scripts                                            |
| `pytest-html`       | Optional: generates HTML reports of test runs                                          |
| `pytest-xdist`      | Optional: allows parallel test execution for faster runs                               |
| `webdriver-manager` | Automatically manages ChromeDriver/GeckoDriver, so you don’t need to download manually |
